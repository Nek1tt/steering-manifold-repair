# Local Windows / VS Code Jupyter additions.
# Install the CUDA-enabled PyTorch wheel FIRST (see LOCAL_WINDOWS_VSCODE.md),
# then install this file. requirements.txt will keep the already installed
# compatible torch version because it only requires torch>=2.2.
-r requirements.txt

# VS Code notebook kernel and notebook-friendly progress bars.
ipykernel>=6.29
ipywidgets>=8.1

# pandas.DataFrame.to_markdown() used by experiment reports.
tabulate>=0.9

# Small environment diagnostics used in the local notebook.
psutil>=6.0
